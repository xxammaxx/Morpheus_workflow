# Experience Bank

Only terminal runs with `verification_pass=true` produce `VERIFIED_EPISODE`
records. Retrieval is bounded and task-class aware. Items are provenance-bound,
expiry/invalidation capable, sanitized against instruction injection and never
grant privileges or authorize providers. A single episode cannot become a
validated heuristic.
