{
  "$id": "provider.paid-escalation.v1",
  "type": "object",
  "required": ["contract", "version", "provider", "model", "reason", "observed_at"],
  "properties": {
    "contract": {"type": "string", "const": "provider.paid-escalation.v1"},
    "version": {"type": "string", "const": "v1"},
    "provider": {"type": "string"},
    "model": {"type": "string"},
    "reason": {"type": "string"},
    "observed_at": {"type": "string"}
  }
}
