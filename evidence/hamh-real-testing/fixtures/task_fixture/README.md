# Task Fixture — Build Task

This repository contains a small Python package with a test suite.
One test is failing. Your task:

1. Inspect the code and the tests.
2. Identify the root cause of the failing test.
3. Fix the bug in `calc.py` ONLY — never modify the test file.
4. Verify by running: `python3 -m pytest test_calc.py -q`
5. Report which test was failing and what the root cause was.

All other tests must stay green. The fix must be minimal.
