# Leitstand-Sicherheit

The dashboard is read-only. Upstream access uses GET-only requests and
mutating HTTP methods return 405. Viewer authentication protects API data and
credentials are not echoed to the page.
