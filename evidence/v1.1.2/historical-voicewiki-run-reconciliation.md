# Historischer VoiceWiki-Lauf

Historical run: `run-mt7l4pyh-alq9oc`. Product delivery remains successful.
The old run is pre-hotfix orchestration history and was reconciled through the
run-scoped Data Table Public API. It now truthfully shows
`BLOCKED/terminal/BLOCKED` with reason code
`HISTORICAL_ORCHESTRATION_STALL_AFTER_BUILD`; product delivery remains
successful.
