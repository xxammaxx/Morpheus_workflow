# State-Chain-Reproduktion

Fresh canonical acceptance run: `run-mt7vbxsy-7ardxd`.
Observed sequence: `BASELINE` → `RESEARCH` (code/docs/tests) → `PLAN` →
`BUILD` → `VERIFY` → `REVIEW` (correctness/security/quality) → `DECIDE` →
`DONE`. Terminal row: `state=DONE`, `current_job=terminal`,
`decision=DONE`, `reason_code=ALL_HARD_GATES_GREEN`.
