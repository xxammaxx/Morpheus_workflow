# Paid Escalation Proof

The offline router test contains only a paid DeepSeek entry and requests
`allow_paid_escalation=true` with reason `NO_FREE_CAPABLE_PROVIDER`. The
decision is marked `paid_escalation=true` and preserves the reason. No paid
network request was made.
